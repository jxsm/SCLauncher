// 应用配置
export interface AppConfig {
  manifestUrl: string
  versionsDir: string
  dataDir: string
  downloadsDir: string
  maxConcurrent: number
  currentVersion: string
  theme: string
  language: string
}
