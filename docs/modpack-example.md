```json
{
    //固定，表示是生存战争的整合包（必须）
  "manifestType": "SurvivalcraftModpack",
    //清单版本号，告诉启动器使用哪个版本的解析器解析(当前只有1个，请填写1)(必填)
   "manifestVersion":1,

    //==============基本信息=======
    "name":"整合包名称", //整合包名称(必填)
    "version": "1.0.0",  //整合包版本(必填)
    "author": "作者名称" //作者(必填)
    "description"： "描述" //简写描述(可选)
    // ============游戏核心配置=============
    "survivalcraft":{
    	"version":{
    		"android":"2.4:api-1.8.2.3", //(可选)如果不填用户在安装时会显示"该整合包不支持android" 
    		"windows":"2.4:api-1.8.2.3"//(可选)如果不填用户在安装时会显示"该整合包不支持windows"
    //============命名规则=============
    //2.4 表示2.4的大版本  api:插件版  1.8.2.3 版本号
    
		}
    
    	"versionList":{ //(可选) 默认使用内置的
    			"android":"URL" //安卓生存战争下载列表清单(可选) 默认使用启动器所选的
    			"windows":"URL" //windows生存战争下载列表(可选) 默认使用启动器所选的
    			// 版本列表中必须包含path,用于下载游戏....
			}

	}
}

```
